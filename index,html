<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anthonys - Homepage (DE)</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background: #000;
            color: #fff;
            overflow-x: hidden;
        }

        /* Navigation */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(0, 0, 0, 0.85);
            backdrop-filter: blur(20px);
            z-index: 1000;
            padding: 12px 0;
            transition: all 0.3s ease;
        }

        .nav-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
        }

        .logo {
            font-size: 20px;
            font-weight: 600;
            color: #fff;
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 30px;
        }

        .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s ease;
        }

        .nav-links a:hover {
            color: #0071e3;
        }

        /* Hero Section */
        .hero {
            height: 70vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(to bottom, #1a1a1a 0%, #1a1a1a 70%, #000 100%);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 100vw;
            height: 100vh;
            background: radial-gradient(ellipse at center, 
                rgba(255, 255, 255, 0.15) 0%, 
                rgba(255, 255, 255, 0.12) 5%, 
                rgba(255, 255, 255, 0.1) 10%, 
                rgba(255, 255, 255, 0.08) 15%, 
                rgba(255, 255, 255, 0.06) 20%, 
                rgba(255, 255, 255, 0.05) 25%, 
                rgba(255, 255, 255, 0.04) 30%, 
                rgba(255, 255, 255, 0.03) 35%, 
                rgba(255, 255, 255, 0.025) 40%, 
                rgba(255, 255, 255, 0.02) 45%, 
                rgba(255, 255, 255, 0.015) 50%, 
                rgba(255, 255, 255, 0.01) 55%, 
                rgba(255, 255, 255, 0.008) 60%, 
                rgba(255, 255, 255, 0.006) 65%, 
                rgba(255, 255, 255, 0.004) 70%, 
                rgba(255, 255, 255, 0.003) 75%, 
                rgba(255, 255, 255, 0.002) 80%, 
                rgba(255, 255, 255, 0.001) 85%, 
                rgba(0, 0, 0, 0.01) 90%, 
                rgba(0, 0, 0, 0.02) 95%, 
                rgba(0, 0, 0, 0.03) 98%, 
                rgba(0, 0, 0, 0.05) 100%);
            border-radius: 50%;
            filter: blur(40px);
            z-index: 1;
        }

        .hero-content {
            text-align: center;
            z-index: 2;
            max-width: 800px;
            padding: 0 20px;
        }

        .hero h1 {
            font-size: clamp(3rem, 8vw, 6rem);
            font-weight: 700;
            margin-bottom: 20px;
            background: linear-gradient(45deg, #fff, #f0f0f0);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            z-index: 2;
            text-shadow: 
                0 0 8px rgba(255, 255, 255, 0.3),
                0 0 16px rgba(255, 255, 255, 0.2);
            filter: drop-shadow(0 0 12px rgba(255, 255, 255, 0.3));
            animation: gentleGlow 4s ease-in-out infinite alternate;
        }

        @keyframes gentleGlow {
            0% {
                text-shadow: 
                    0 0 8px rgba(255, 255, 255, 0.3),
                    0 0 16px rgba(255, 255, 255, 0.2);
                filter: drop-shadow(0 0 12px rgba(255, 255, 255, 0.3));
            }
            100% {
                text-shadow: 
                    0 0 12px rgba(255, 255, 255, 0.4),
                    0 0 20px rgba(255, 255, 255, 0.25);
                filter: drop-shadow(0 0 16px rgba(255, 255, 255, 0.4));
            }
        }

        .hero p {
            font-size: clamp(1.2rem, 3vw, 1.8rem);
            margin-bottom: 40px;
            opacity: 0.9;
            position: relative;
            z-index: 2;
        }



        /* Liquid Glass Section */
        .liquid-glass {
            padding: 120px 0;
            background: linear-gradient(180deg, #000 0%, #1a1a1a 100%);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .section-title {
            font-size: clamp(2.5rem, 6vw, 4rem);
            font-weight: 700;
            text-align: center;
            margin-bottom: 60px;
            background: linear-gradient(45deg, #fff, #ccc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .glass-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            margin-bottom: 80px;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            padding: 40px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .glass-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.15);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .glass-card h3 {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: #fff;
        }

        .glass-card p {
            color: #ccc;
            line-height: 1.6;
        }

        /* Features Section */
        .features {
            padding: 120px 0;
            background: #000;
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 60px;
            align-items: center;
        }

        .feature-text h3 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #fff;
        }

        .feature-text p {
            font-size: 1.2rem;
            color: #ccc;
            line-height: 1.6;
        }

        .feature-visual {
            height: 400px;
            background: linear-gradient(135deg, #0071e3, #00a8ff);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .feature-visual::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="40" fill="none" stroke="rgba(255,255,255,0.3)" stroke-width="2"/></svg>');
            animation: rotate 20s linear infinite;
        }

        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Visual Intelligence Section */
        .visual-intelligence {
            padding: 120px 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .vi-content {
            text-align: center;
            margin-bottom: 80px;
        }

        .vi-content h2 {
            font-size: 3rem;
            margin-bottom: 20px;
        }

        .vi-content p {
            font-size: 1.3rem;
            opacity: 0.9;
            max-width: 800px;
            margin: 0 auto;
        }

        /* Interactive Elements */
        .interactive-demo {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
            margin-top: 60px;
        }

        .demo-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .demo-card:hover {
            transform: scale(1.05);
            background: rgba(255, 255, 255, 0.2);
        }

        .demo-icon {
            width: 60px;
            height: 60px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        /* Messages Section */
        .messages-section {
            padding: 120px 0;
            background: #1a1a1a;
        }

        .messages-demo {
            background: #2a2a2a;
            border-radius: 25px;
            padding: 40px;
            max-width: 600px;
            margin: 0 auto;
        }

        .message-bubble {
            background: #0084ff;
            color: white;
            padding: 15px 20px;
            border-radius: 20px;
            margin: 10px 0;
            max-width: 70%;
            animation: slideIn 0.5s ease-out;
        }

        .message-bubble.received {
            background: #444;
            margin-left: auto;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* CTA Section */
        .cta-section {
            padding: 120px 0;
            background: linear-gradient(135deg, #0071e3, #00a8ff);
            text-align: center;
        }

        .cta-button {
            display: inline-block;
            background: #fff;
            color: #0071e3;
            padding: 15px 40px;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            margin: 20px 10px;
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        .cta-button.secondary {
            background: transparent;
            color: #fff;
            border: 2px solid #fff;
        }

        .cta-button.secondary:hover {
            background: #fff;
            color: #0071e3;
        }

        /* Footer */
        .footer {
            background: #1a1a1a;
            padding: 60px 0 40px;
            color: #999;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }

        .footer-section h4 {
            color: #fff;
            margin-bottom: 20px;
            font-size: 1.1rem;
        }

        .footer-section ul {
            list-style: none;
        }

        .footer-section ul li {
            margin-bottom: 10px;
        }

        .footer-section ul li a {
            color: #999;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer-section ul li a:hover {
            color: #0071e3;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }
            
            .hero h1 {
                font-size: 3rem;
            }
            
            .feature-grid {
                grid-template-columns: 1fr;
                gap: 40px;
            }
            
            .glass-grid {
                grid-template-columns: 1fr;
            }
            
            .interactive-demo {
                grid-template-columns: 1fr;
            }
        }

        /* Smooth Scrolling */
        html {
            scroll-behavior: smooth;
        }

        /* Parallax Effect */
        .parallax {
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }

        /* Loading Animation */
        .loading-spinner {
            width: 40px;
            height: 40px;
            border: 4px solid rgba(255, 255, 255, 0.3);
            border-top: 4px solid #0071e3;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 20px auto;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-content">
            <a href="#" class="logo">Apple</a>
            <ul class="nav-links">
                <li><a href="#home">Home</a></li>
                <li><a href="#features">Features</a></li>
                <li><a href="#intelligence">Intelligence</a></li>
                <li><a href="#apps">Apps</a></li>
                <li><a href="#support">Support</a></li>
            </ul>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-content">
            <h1>Anthonys</h1>
            <p>Homepage. Der Löwe. Ein echter Berliner.</p>
        </div>
    </section>

    <!-- Liquid Glass Section -->
    <section class="liquid-glass" id="features">
        <div class="container">
            <h2 class="section-title">Ein neues Design.</h2>
            <p style="text-align: center; font-size: 1.3rem; color: #ccc; margin-bottom: 60px; max-width: 800px; margin-left: auto; margin-right: auto;">
                Der Liquid Glass Look mit seinem fließenden Charakter und der optischen Qualität von Glas liefert ein ausdrucksstarkes neues Design für deinen Sperrbildschirm, deinen Homescreen, dein Kontrollzentrum, deine Apps und mehr.
            </p>
            
            <div class="glass-grid">
                <div class="glass-card">
                    <h3>Auf deinem Sperrbildschirm ist was los.</h3>
                    <p>Die Ziffern der Zeitanzeige passen sich dynamisch deinem Hintergrundbild und Mitteilungen an, damit dein Motiv immer sichtbar bleibt.</p>
                </div>
                <div class="glass-card">
                    <h3>Neue App Symbole.</h3>
                    <p>Wähle aus aktualisierten hellen oder dunklen Designs, neu eingefärbten Symbolen und einem atemberaubend klaren Look.</p>
                </div>
                <div class="glass-card">
                    <h3>Dynamische Steuerelemente.</h3>
                    <p>Über Apps hinweg reflektieren und brechen die Liquid Glass Steuerelemente ihre Umgebung.</p>
                </div>
                <div class="glass-card">
                    <h3>Ein einheitlicher Look im ganzen System.</h3>
                    <p>Liquid Glass sorgt für ein besonderes Erlebnis über Apps und Geräte hinweg.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Visual Intelligence Section -->
    <section class="visual-intelligence" id="intelligence">
        <div class="container">
            <div class="vi-content">
                <h2>Mach mehr mit visueller Intelligenz.</h2>
                <p>Mit visueller Intelligenz auf dem iPhone kannst du jetzt etwas suchen, Fragen stellen und mit Inhalten auf deinem Display interagieren.</p>
            </div>
            
            <div class="interactive-demo">
                <div class="demo-card">
                    <div class="demo-icon">🔍</div>
                    <h3>Live Übersetzung</h3>
                    <p>Texte in Nachrichten automatisch übersetzen lassen</p>
                </div>
                <div class="demo-card">
                    <div class="demo-icon">🎨</div>
                    <h3>Image Playground</h3>
                    <p>Kombiniere Emojis und Beschreibungen für völlig neue Bilder</p>
                </div>
                <div class="demo-card">
                    <div class="demo-icon">⚡</div>
                    <h3>Intelligente Aktionen</h3>
                    <p>Workflows in Kurzbefehlen mit Apple Intelligence</p>
                </div>
                <div class="demo-card">
                    <div class="demo-icon">📞</div>
                    <h3>Anruffilter</h3>
                    <p>Automatische Verwaltung unerwünschter Anrufe</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="container">
            <div class="feature-grid">
                <div class="feature-text">
                    <h3>Individualisiere deine Chats</h3>
                    <p>Füge Unterhaltungen Hintergründe hinzu. Wähle aus tollen, integrierten Optionen oder entscheide dich für deine eigenen Fotos und gib deinen Unterhaltungen in Nachrichten einen persönlichen Touch.</p>
                </div>
                <div class="feature-visual">
                    <div class="loading-spinner"></div>
                </div>
            </div>
        </div>
    </section>

    <!-- Messages Demo Section -->
    <section class="messages-section">
        <div class="container">
            <h2 class="section-title">Starte Umfragen in Nachrichten</h2>
            <div class="messages-demo">
                <div class="message-bubble">Hey! Wo sollen wir uns heute treffen? 🍕</div>
                <div class="message-bubble received">Erstelle eine Umfrage!</div>
                <div class="message-bubble">Pizza Place oder Burger Joint?</div>
                <div class="message-bubble received">Pizza Place! 🍕</div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="container">
            <h2 style="font-size: 3rem; margin-bottom: 20px;">Erlebe Anthonys</h2>
            <p style="font-size: 1.3rem; margin-bottom: 40px; opacity: 0.9;">
                Entdecke alle neuen Features und Verbesserungen
            </p>
            <a href="#" class="cta-button">Mehr erfahren</a>
            <a href="#" class="cta-button secondary">Update laden</a>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h4>Shop and Learn</h4>
                <ul>
                    <li><a href="#">Mac</a></li>
                    <li><a href="#">iPad</a></li>
                    <li><a href="#">iPhone</a></li>
                    <li><a href="#">Watch</a></li>
                    <li><a href="#">AirPods</a></li>
                    <li><a href="#">TV & Home</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>Services</h4>
                <ul>
                    <li><a href="#">Apple Music</a></li>
                    <li><a href="#">Apple TV+</a></li>
                    <li><a href="#">Apple Fitness+</a></li>
                    <li><a href="#">Apple News+</a></li>
                    <li><a href="#">Apple Arcade</a></li>
                    <li><a href="#">iCloud</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>Account</h4>
                <ul>
                    <li><a href="#">Manage Your Apple ID</a></li>
                    <li><a href="#">Apple Store Account</a></li>
                    <li><a href="#">iCloud.com</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>Apple Store</h4>
                <ul>
                    <li><a href="#">Find a Store</a></li>
                    <li><a href="#">Genius Bar</a></li>
                    <li><a href="#">Today at Apple</a></li>
                    <li><a href="#">Apple Camp</a></li>
                </ul>
            </div>
        </div>
    </footer>

    <script>
        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Navbar background change on scroll
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 100) {
                navbar.style.background = 'rgba(0, 0, 0, 0.95)';
            } else {
                navbar.style.background = 'rgba(0, 0, 0, 0.85)';
            }
        });

        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe all cards and sections
        document.querySelectorAll('.glass-card, .demo-card, .feature-text').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'all 0.6s ease';
            observer.observe(el);
        });

        // Interactive hover effects
        document.querySelectorAll('.glass-card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-10px) scale(1.02)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });

        // Demo card interactions
        document.querySelectorAll('.demo-card').forEach(card => {
            card.addEventListener('click', function() {
                this.style.animation = 'none';
                setTimeout(() => {
                    this.style.animation = 'slideIn 0.5s ease-out';
                }, 10);
            });
        });

        // Parallax effect for hero section
        window.addEventListener('scroll', function() {
            const scrolled = window.pageYOffset;
            const hero = document.querySelector('.hero');
            const rate = scrolled * -0.5;
            hero.style.transform = `translateY(${rate}px)`;
        });

        // Dynamic message animation
        function addMessage() {
            const messagesDemo = document.querySelector('.messages-demo');
            const messages = [
                'AutoMix in Apple Music ist fantastisch! 🎵',
                'Die neuen Liquid Glass Symbole sehen toll aus! ✨',
                'Visuelle Intelligenz macht alles so viel einfacher 🤖'
            ];
            
            const randomMessage = messages[Math.floor(Math.random() * messages.length)];
            const newBubble = document.createElement('div');
            newBubble.className = 'message-bubble';
            newBubble.textContent = randomMessage;
            
            messagesDemo.appendChild(newBubble);
            
            // Remove old messages to prevent overflow
            const bubbles = messagesDemo.querySelectorAll('.message-bubble');
            if (bubbles.length > 6) {
                bubbles[0].remove();
            }
        }

        // Add new message every 5 seconds
        setInterval(addMessage, 5000);

        // CTA button interactions
        document.querySelectorAll('.cta-button').forEach(button => {
            button.addEventListener('click', function(e) {
                e.preventDefault();
                
                // Create ripple effect
                const ripple = document.createElement('span');
                const rect = this.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                const x = e.clientX - rect.left - size / 2;
                const y = e.clientY - rect.top - size / 2;
                
                ripple.style.width = ripple.style.height = size + 'px';
                ripple.style.left = x + 'px';
                ripple.style.top = y + 'px';
                ripple.style.position = 'absolute';
                ripple.style.borderRadius = '50%';
                ripple.style.background = 'rgba(255, 255, 255, 0.3)';
                ripple.style.transform = 'scale(0)';
                ripple.style.animation = 'ripple 0.6s linear';
                ripple.style.pointerEvents = 'none';
                
                this.style.position = 'relative';
                this.style.overflow = 'hidden';
                this.appendChild(ripple);
                
                setTimeout(() => {
                    ripple.remove();
                }, 600);
            });
        });

        // Add ripple animation CSS
        const style = document.createElement('style');
        style.textContent = `
            @keyframes ripple {
                to {
                    transform: scale(4);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>
